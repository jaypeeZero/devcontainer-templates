{
	"name": "Container Name",
	"image": "mcr.microsoft.com/devcontainers/base:ubuntu",
	// enable docker-in-docker to run docker inside your devcontainer
	//"features": {
		//"ghcr.io/devcontainers/features/docker-in-docker:1": {}
	//},
	"remoteUser": "vscode", // force the user to be 'vscode'
	"updateRemoteUserUID": true, // update the remote user's id to match the host user id
	"postCreateCommand": "./post-install.sh",
	"customizations": {
		"vscode": {
			"extensions": [
				"some-extension-id"
			]
		}
	}
}
